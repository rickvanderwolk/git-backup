# GitHub Backup Configuration

# Your GitHub username (required)
GITHUB_USER="your-github-username"

# GitHub Personal Access Token (optional for public repos)
# If empty, only public repos will be backed up with rate limit of 60/hour
# With token: 5000 requests/hour and access to private repos
# Create token at: https://github.com/settings/tokens (needs 'repo' scope for private repos)
GITHUB_TOKEN=""

# Backup target directories (space-separated list)
# These should be mount points of your USB drives
# Script will create a 'git-backup' subdirectory in each target
# Script will skip targets that are not mounted
BACKUP_TARGETS="/mnt/usb1 /mnt/usb2"

# Master backup directory on Pi (persistent storage for incremental updates)
# This directory will hold the master copies of all git mirrors
# Repos are updated here first, then synced to USB drives
MASTER_BACKUP_DIR="/home/pi/git-backup-master"

# Temporary directory for creating working copies (if enabled)
TMP_DIR="/tmp/git-backup"

# Create working copies (browsable files) on USB drives
# 'true'  = Mirror + working copy on USB (uses ~2x space, files directly readable)
# 'false' = Only mirror on USB (compact, need git clone to read files)
CREATE_WORKING_COPY="false"
